//  this variable will help us to count the number of likes
var likeCount = 0;
// the function of this code block would be a loop to print out how many likes there are
function increaseLikes() {
    // this sort operation is for the number of like?
    likeCount = likeCount + 1;
}
